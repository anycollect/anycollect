extenstions:
- !load
  extension: PullEngine
  instance: puller
  config:
    updatePeriod: 30
    pullPeriod: 15
    defaultPoolSize: 1
    rules:
    - targetId: "app1-.*"
      poolSize: 3
    - targetId: "app2-.*"
      poolSize: 2